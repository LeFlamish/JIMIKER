org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# 빌드에 쓸 자바를 Android Studio 내장 JDK(21)로 고정한다.
# 시스템에 더 새 자바(예: JDK 25)가 깔려 있으면 Gradle이 아직 지원하지
# 않아 빌드가 깨지기 때문이다. 다른 PC에서 빌드할 때 이 경로가 다르면
# Android Studio → Settings → Build Tools → Gradle의 "Gradle JDK" 경로로
# 고쳐 쓴다. (경로 구분자는 윈도우에서도 / 를 쓴다)
org.gradle.java.home=C:/Program Files/Android/Android Studio/jbr
